let express,testController,testModel;_f81‍.x([["default",()=>_f81‍.o]]);_f81‍.w("express",[["default",["express"],function(v){express=v}]]);_f81‍.w("../controller/testController",[["default",["testController"],function(v){testController=v}]]);_f81‍.w("../models/testModel",[["default",["testModel"],function(v){testModel=v}]]);


const test = express.Router();

test.get("/", testController.readData);
test.get("/:id",testController);

test.post("/", async (req, res) => {
  try {
    _f81‍.g.console.log(req.body);
    const data = await testModel.create(req.body);
    res.status(200).json({
      message: "Connected",
      success: true,
      data: data,
    });
  } catch (error) {
    res.status(500).send(error);
  }
});
test.put("/:id", async (req, res) => {
  try {
    _f81‍.g.console.log(req.params.id);
    const data = await testModel.findByIdAndUpdate(req.params.id, req.body, {
      new: true,
    });
    res.status(200).json({
      message: "Connected",
      data: data,
    });
  } catch (err) {
    res.status(500).send(err);
  }
});
test.delete("/:id", async (req, res) => {
  try {
    _f81‍.g.console.log(req.params.id);
    const data = await testModel.findByIdAndDelete(req.params.id);
    res.status(200).json({
      message: "Connected",
      data: data,
    });
  } catch (err) {
    res.status(500).send(err);
  }
});

_f81‍.d(test);
