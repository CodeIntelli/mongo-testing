let mongoose;_746‍.x([["default",()=>_746‍.o]]);_746‍.w("mongoose",[["default",["mongoose"],function(v){mongoose=v}]]);

let testSchema = new mongoose.Schema({
  name: {
    required: true,
    type: String,
    
  },
  contact: {
    type: Number,
    required: true,
    min: [10, "This is the min message"],
    max: [12, "This is the max message"],
  },
  isActive: {
    type: Boolean,
    require: true,
  },
});

const testModel = new mongoose.model("test", testSchema);

_746‍.d(testModel);
